part of 'home_main_widgets_imports.dart';
class BuildHomeCategories extends StatelessWidget {
  const BuildHomeCategories({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
