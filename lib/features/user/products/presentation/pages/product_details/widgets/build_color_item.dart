part of  'product_details_widgets_imports.dart';

class BuildColorItem extends StatelessWidget {
  const BuildColorItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
