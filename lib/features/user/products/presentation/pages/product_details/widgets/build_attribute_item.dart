part of 'product_details_widgets_imports.dart';

class BuildAttributeItem extends StatelessWidget {
  const BuildAttributeItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
