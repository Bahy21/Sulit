part of 'product_details_widgets_imports.dart';
class BuildProductInfo extends StatelessWidget {
  const BuildProductInfo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
