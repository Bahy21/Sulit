part of 'product_details_widgets_imports.dart';
class BuildShareProduct extends StatelessWidget {
  const BuildShareProduct({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
